#include <iostream>
#include <cmath>
// 8 usefull math related functions

int main()
{
    double v = 3.99;
    double w = 4.13;
    double x = 3;
    double y = 4;
    double z;

    // 'max' function = will return the greater of 2 values or variables
    // 'min function = will return the lower of 2 values or variable

    
/*      z = std::max(x, y);
        std::cout << z;
        Answer: 4

        z = std::min(x, y);
        std::cout << z;
        Answer: 3


    'pow' function = to raise a base to a given power

        z = pow(2, 4); 
        std::cout << z;
        Answer: 16


    'sqrt' function = used to get the square root

        z = sqrt(9);
        std::cout << z;
        Answer: 3


    'abs' function = used to get the absolute value 

        z = abs(-9);
        std::cout << z;
        Answer: 9


    'round' function = used to round off a number

        z = round(w);
        std::cout << z;
        Answer: 4
    

    'ceil' function = rounds up the number
        z = ceil(w);
        std::cout << z;
        Answer: 5


    'floor' function = rounds the number down
        z = floor(v);
        std::cout << z;
        Answer: 3
*/

    
    return 0;
}

// FOR MORE MATH RELATED FUNCTIONS GO TO cplusplus.com/reference/cmath

